[project]
name = "fondry-backend"
version = "0.1.0"
description = "ontology-fondry backend: LLM-native enterprise knowledge graph platform"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
    "openai>=1.40",
    "pypdf>=4.0",
    "python-multipart>=0.0.9",
    "fastmcp>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
